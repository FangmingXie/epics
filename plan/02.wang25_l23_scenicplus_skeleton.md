# Skeleton Plan: SCENIC+ on Wang et al. 2025 (human), L2/3 neurons only

> **Status: rough skeleton.** High-level stages + open questions to resolve before a
> detailed, executable plan is written. Not yet ready to run.

## Context / goal

Run the (now-working) SCENIC+ pipeline in the `epics` env on the epigenomics data from
**Wang et al. 2025 (Nature)** — a **developing human brain** atlas — restricted to
**L2/3 excitatory neurons**. Produce an eGRN (eRegulons) for that population.

What we know from data already on disk (`/data/qlyu/data/wang25_human/`, MERFISH portion):
- Developing human cortex; samples span 2nd trimester → infancy; regions V1/BA17/BA10/PFC/MFG.
- Cell-type annotation scheme: **L2/3 = `type == "EN-L2_3-IT"`** (22,667 cells in MERFISH),
  niche "Cortical layer 2 and 3". This is the label to select on (or its equivalent in the
  epigenomics object).

Key differences from the prior mouse pipeline (v1_astro / the `epics` test run):
- **Species = human → hg38**, human motif collection (**hgnc**), human cisTarget database.
  The proven test used mm10/mouse assets; those must all be swapped to human.

## ⚠️ Open questions (resolve before detailing)

1. **Epigenomics data source.** Only the MERFISH (spatial RNA) is on disk — SCENIC+ needs
   **chromatin accessibility**. Which modality does the paper provide, and where?
   - Paired **multiome** (RNA+ATAC same nuclei) → ideal for SCENIC+.
   - Separate **snATAC** + **snRNA** → needs label transfer / pseudo-multiome pairing.
   - Need the **accession** (GEO/BICAN/portal) + files: fragments (`.tsv.gz`), per-cell
     metadata with the `EN-L2_3-IT` label, and the RNA matrix.
2. **cisTarget ranking DB (hg38).** Not on disk. Either (a) download a prebuilt hg38 screen
   database, or (b) build a custom DB from the Wang consensus peaks (builder code exists at
   `/data/qlyu/code/trial/create_cisTarget_databases/`). Which?
3. **Scope of "L2/3".** Just `EN-L2_3-IT`, or include immature/related (`EN-IT-Immature`,
   `EN-Newborn`)? Which developmental stages/regions to include?

## Skeleton stages

- **Stage 0 — Acquire data.** Download Wang 2025 epigenomics (ATAC/multiome) + cell metadata
  → `epics/data/wang25/` (gitignored). Resolve modality (Q1).
- **Stage 1 — Select L2/3.** Subset cells to `EN-L2_3-IT` (Q3). Emit the L2/3 barcode list,
  RNA `.h5ad`, and ATAC fragments restricted to those barcodes.
- **Stage 2 — Human references.** Assemble hg38 assets: genome fasta (on disk), chrom.sizes,
  blacklist, hgnc motif tbl (on disk: `.../motifs-v10-nr.hgnc-m0.00001-o0.0.tbl`), and the
  hg38 cisTarget ranking/score DB (Q2).
- **Stage 3 — pycisTopic.** Peaks (MACS on L2/3 fragments) → cisTopic object → LDA topic
  models → candidate enhancer region sets. (Mirrors astro `11.run_pycistopic.py`.)
- **Stage 4 — SCENIC+ prepare_data.** `scenicplus prepare_GEX_ACC` (build multiome MuData),
  `prepare_menr` + motif enrichment (cistarget/DEM) using the human DB, `search_space`.
- **Stage 5 — GRN inference.** `scenicplus grn_inference {TF_to_gene, region_to_gene, eGRN}`
  → L2/3 eRegulons. (Same CLI verified in the `epics` test run.)
- **Stage 6 — QC / sanity.** eRegulon count, known L2/3 TFs (e.g. CUX2, SATB2, POU3F2/BRN2),
  AUCell, basic plots.

## Reusable assets

- `epics` env (SCENIC+ 1.0a2, verified end-to-end).
- Verified CLI pattern + scripts: `scripts/test02_make_mini_mudata.py`, `scripts/test03_run_grn.sh`.
- Prior full mouse pipeline as a template: `/data/qlyu/code/v1astro/run_scenic/`
  (snakemake config, pycisTopic, peak calling) — adapt paths/params mouse→human.
- cisTarget DB builder: `/data/qlyu/code/trial/create_cisTarget_databases/`.
- hg38 fasta: `/home/qlyu/mydata/data/v1_multiome_ai/chrombpnet_tutorial/data/downloads/hg38.fa`.
- Human hgnc motif tbl (on disk, see Stage 2).

## Next step

Resolve the three open questions (chiefly Q1: locate the epigenomics data + accession),
then expand this skeleton into a detailed, executable plan with concrete paths and params.

## Sources (paper identification, tentative)

- Wang25 MERFISH data on disk: `/data/qlyu/data/wang25_human/README.md` (developing human brain).
- Candidate related atlases (developing human brain multiome), to confirm exact paper/accession:
  [BICAN developing human brain collection](https://www.nature.com/collections/gjdefhadcj),
  [Single-cell multiomic human brain atlas (Nat Commun 2026)](https://www.nature.com/articles/s41467-026-69368-2).
